import { ipcMain } from 'electron'
import db from '../../db/lowdb.js'
import { IPC_CHANNELS } from '../channels'
import { Achievement } from '../../db/types'

export function registerAchievementHandlers() {
  // TODO -> Achievements not part of MVP
}
